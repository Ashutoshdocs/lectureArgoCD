apiVersion: v1
kind: Service
metadata:
  name: web
  labels: { app: web, tier: cluster-demo }
spec:
  type: ClusterIP
  selector: { app: web, tier: cluster-demo }
  ports: [{ name: http, port: 80, targetPort: 80 }]
