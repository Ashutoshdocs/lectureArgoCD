apiVersion: v1
kind: Service
metadata:
  name: nginx-dev
  namespace: default
  labels:
    app: web
    env: dev
spec:
  type: NodePort
  selector:
    app: web
    env: dev
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080   # access at http://<dev-vm-ip>:30080
