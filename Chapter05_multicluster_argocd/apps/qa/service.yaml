apiVersion: v1
kind: Service
metadata:
  name: nginx-qa
  namespace: default
  labels:
    app: web
    env: qa
spec:
  type: NodePort
  selector:
    app: web
    env: qa
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080   # access at http://<qa-vm-ip>:30080
