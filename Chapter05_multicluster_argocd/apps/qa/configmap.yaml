apiVersion: v1
kind: ConfigMap
metadata:
  name: web-content
  namespace: default
  labels:
    app: web
    env: qa
data:
  index.html: |
    <!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="UTF-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <title>QA • ArgoCD Multicluster</title>
      <style>
        :root{color-scheme:dark}
        *{box-sizing:border-box}
        body{margin:0;min-height:100vh;display:flex;align-items:center;justify-content:center;
             font-family:system-ui,-apple-system,"Segoe UI",Roboto,sans-serif;
             background:radial-gradient(circle at 50% 15%,#f59e0b 0%,#d97706 40%,#7c2d12 100%);
             color:#fff7ed}
        .card{padding:3rem 3.75rem;border-radius:22px;background:rgba(0,0,0,.28);
              border:1px solid rgba(255,255,255,.18);text-align:center;
              box-shadow:0 24px 70px rgba(0,0,0,.45);max-width:520px}
        .tag{display:inline-block;padding:.35rem 1rem;border-radius:999px;font-weight:800;
             letter-spacing:2px;background:#fbbf24;color:#5a2c00;font-size:.8rem}
        h1{margin:.9rem 0 .3rem;font-size:3rem;text-shadow:0 2px 20px rgba(251,191,36,.6)}
        p{margin:.35rem 0;color:#ffedd5}
        .meta{margin-top:1.4rem;font-size:.85rem;color:#fed7aa;border-top:1px solid rgba(255,255,255,.15);padding-top:1rem}
        code{background:rgba(255,255,255,.12);padding:.1rem .4rem;border-radius:6px}
      </style>
    </head>
    <body>
      <div class="card">
        <span class="tag">QA / STAGING</span>
        <h1>🧪 QA Cluster</h1>
        <p>Test, verify, sign off. The last gate before production.</p>
        <p>Deployed by <strong>ArgoCD</strong> — same Git, different cluster.</p>
        <div class="meta">
          Environment: <code>qa</code> &nbsp;•&nbsp; App: <code>nginx-qa</code><br/>
          Managed by ArgoCD • GitOps ✨
        </div>
      </div>
    </body>
    </html>

  default.conf: |
    server {
      listen 80;
      server_name _;
      location / { root /usr/share/nginx/html; index index.html; }
    }
