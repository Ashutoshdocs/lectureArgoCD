apiVersion: v1
kind: Service
metadata:
  name: web
  labels: { app: web, env: prod }
spec:
  type: NodePort
  selector: { app: web, env: prod }
  ports:
    - { name: http, port: 80, targetPort: 80, nodePort: 30083 }   # http://<node-ip>:30083
