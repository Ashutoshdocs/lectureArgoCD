apiVersion: v1
kind: Service
metadata:
  name: web
  labels: { app: web, env: dev }
spec:
  type: NodePort
  selector: { app: web, env: dev }
  ports:
    - { name: http, port: 80, targetPort: 80, nodePort: 30081 }   # http://<node-ip>:30081
