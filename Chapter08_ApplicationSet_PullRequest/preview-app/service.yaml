apiVersion: v1
kind: Service
metadata:
  name: web
  labels: { app: web, tier: preview }
spec:
  type: ClusterIP
  selector: { app: web, tier: preview }
  ports: [{ name: http, port: 80, targetPort: 80 }]
