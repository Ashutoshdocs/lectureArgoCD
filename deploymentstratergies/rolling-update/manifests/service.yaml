apiVersion: v1
kind: Service
metadata:
  name: rolling-demo
  namespace: rolling-update
spec:
  type: NodePort
  selector:
    app: rolling-demo
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30081     # browser: http://<node-ip>:30081
