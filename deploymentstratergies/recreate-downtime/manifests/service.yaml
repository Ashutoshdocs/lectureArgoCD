apiVersion: v1
kind: Service
metadata:
  name: downtime-demo
  namespace: recreate-downtime
spec:
  type: NodePort
  selector:
    app: downtime-demo
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30082     # browser: http://<node-ip>:30082
